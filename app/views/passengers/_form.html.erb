<%= form_for(@passenger, html: {class: 'form-inline'}) do |f| %>
  <% if @passenger.errors.any? %>
  <div class="alert alert-warning alert-dismissible" role="alert">
    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    <h3><%= pluralize(@passenger.errors.count, "error") %> prohibited this booking from being saved:</h3>
    <ul>
    <% @passenger.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, class: 'sr-only' %>
    <%= f.text_field :name, class: 'form-control', placeholder: "Name" %>
  </div>
  <div class="form-group">
    <%= f.label :email, class: 'sr-only' %>
    <%= f.email_field :email, class: 'form-control', placeholder: "Email" %>
  </div>
  <div class="form-group">
    <%= f.label :phone, class: 'sr-only' %>
    <%= f.text_field :phone, class: 'form-control', placeholder: "Phone" %>
  </div>
  <div class="form-group">
    <%= f.label :address, class: 'sr-only' %>
    <%= f.text_field :address, class: 'form-control', placeholder: "Address" %>
  </div>
  <% if @flight %>
    <%= f.hidden_field :flight_id, value: @flight.id %>
  <% end %>
    <%= f.submit %>
<% end %>
